# Expo SDK 57 ships a transitive react/react-dom peer skew (react-dom pulled in for
# web tooling wants a newer react than the SDK pins). legacy-peer-deps makes
# `npm install` and `npm ci` resolve identically so CI stays green.
legacy-peer-deps=true
